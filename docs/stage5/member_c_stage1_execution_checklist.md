# Member C Stage1 Execution Checklist

## 0) Start Gate (must be true before coding)
- [ ] Stage0 is completed and merged on main.
- [ ] A/B/C sign-off exists for Stage0 gate.
- [ ] Go/No-Go gate is marked as GO.

## 1) Stage1 Scope Lock (do only this)
- [x] Only implement minimal evidence logs (4 classes / 5 files).
- [x] Do not add full stage log / dashboard / scoring system.
- [x] Do not perform large Provider architecture refactor in Stage1.

## 2) Required Deliverables (4 classes / 5 files)
- [x] `logs/raw_news_ingest.jsonl`
- [x] `logs/market_data_provenance.jsonl`
- [x] `logs/decision_gate.jsonl`
- [x] `logs/replay_write.jsonl`
- [x] `logs/execution_emit.jsonl`

## 3) Mandatory Traceability Fields (C side)
- [x] `trace_id` is present and consistent across all 5 files.
- [x] `event_trace_id` is present in replay-related logs.
- [x] `request_id` is present where request-scoped.
- [x] `batch_id` is present where batch-scoped.
- [x] `event_hash` is present in replay/join evidence.

## 4) Replay/Join Integrity (C implementation responsibility)
- [x] Implement writer path for replay evidence.
- [ ] Implement join validator with required key checks.
- [ ] Ensure replay primary-key completeness can be measured.
- [ ] Ensure orphan replay can be detected and reported.

## 5) Decision/Gate Evidence Requirements
- [x] Decision gate log records allow/block outcome.
- [x] Decision gate log records blocker reasons.
- [x] Market provenance log records source/present/stale/default/fallback.
- [x] Execution emit log can be joined back to replay via trace keys.

## 6) Non-Regression & Safety Checks
- [ ] Retries do not duplicate replay writes.
- [ ] Retries do not duplicate execution emits.
- [ ] Output can answer:
  - [x] what was ingested,
  - [x] whether market data was trustworthy,
  - [x] why gate allowed/blocked,
  - [x] whether replay and execution diverged.

## 7) Collaboration Rules (A/B/C boundaries)
- [x] Do not modify state-machine enums (A-owned).
- [x] Do not rename contract fields without joint review (A-owned naming).
- [x] Keep schema/config touchpoints under joint review when required.

## 8) Verification Commands (before PR)
- [x] Run targeted tests covering Stage1 evidence and gate paths.
- [x] Spot-check generated JSONL lines for trace continuity.
- [x] Verify no out-of-scope Stage2/4/5 features were added.

## 9) PR Exit Criteria for C (Stage1)
- [x] 5 log files are generated by runtime path.
- [x] Trace keys are end-to-end joinable.
- [x] A confirms schema/contract compatibility (`docs/stage5/member_a_stage1_contract_field_spec.md`).
- [x] B confirms fields are consumable for mapping acceptance.

## 10) Suggested Commit/PR Naming
- Branch: `feat/member-c-stage1-evidence-logs`
- PR title: `feat(stage1/c): minimal evidence logs with trace-join continuity`
