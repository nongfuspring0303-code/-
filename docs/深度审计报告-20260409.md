# 事件驱动交易模块阶段二：深度审计报告（管理层版 + 工程执行版）

审计时间：2026-04-09  
审计范围：`/Users/workmac/.openclaw/workspace/事件驱动交易模块阶段二`  
审计方式：代码静态审计 + 测试验证 + 健康检查 + 配置契约核对

---

## 一、管理层版（给决策者）

### 1) 总体结论
- 当前项目达到“**阶段三可联调、可回放、可测试**”水平。
- 尚未达到“**阶段四生产前验收通过**”水平。
- 结论：**条件性通过阶段三，不合格于阶段四最终验收**。

### 2) 评分卡（本次新基准）
| 维度 | 得分 | 结论 |
|---|---:|---|
| 安全与访问控制 | 82 | 基础合格（WS/API/XSS 已修） |
| 数据真实性与时效 | 48 | 不合格（实时行情/强失败未闭环） |
| 裁决准确性与一致性 | 55 | 不合格（语义层/主裁决未落地） |
| 可观测与可恢复 | 78 | 较好（健康检查和证据台账可用） |
| 工程质量与测试 | 84 | 较好（154 通过） |
| 生产化治理 | 62 | 未达标（版本统一/持久化/多实例治理缺口） |

综合评分：**68/100**

### 3) 风险分级
- **P0（必须先修）**
  - 实时行情未接入（VIX/SPX/实时价）
  - 源失败仍可 fallback 产出测试事件
  - 静态 `last_price` 仍参与交易区间
- **P1（本阶段内修复）**
  - 多实例主裁决（Master-Worker）未落地
  - EventBus 历史不落盘
  - schema 版本默认值不统一
- **P2（治理收口）**
  - websockets 弃用告警
  - 回归门禁指标未全部进入 CI

### 4) 是否建议进入阶段四收尾
- 建议进入，但必须按“P0 -> P1 -> P2”推进，且以指标验收收口。

---

## 二、工程执行版（给研发）

### 1) 证据摘要
- 测试：`python3 -m pytest -q` → **154 passed, 2 warnings**（复跑结果稳定）
- 健康检查：`python3 scripts/system_healthcheck.py` → **OVERALL GREEN**
- 证据台账：`logs/system_health_report.json` 显示 **live_run_count=0**（仅 replay 证据）

关键证据文件：
- `logs/system_health_report.json`
- `scripts/event_bus.py`
- `canvas/app.js`
- `scripts/data_adapter.py`
- `scripts/ai_event_intel.py`
- `configs/premium_stock_pool.yaml`

### 2) 已达成项（确认）
- WebSocket 鉴权已实现：`scripts/event_bus.py:90`, `scripts/event_bus.py:118`
- Config API 鉴权已实现：`scripts/config_api_server.py:54`, `scripts/config_api_server.py:65`
- XSS 防护已实现：`canvas/app.js:32`（`escapeHtml`）
- 前端缓存治理已实现：`canvas/app.js:10`, `canvas/app.js:76`, `canvas/app.js:265`
- 市值阈值冲突已对齐（50B）：`configs/premium_stock_pool.yaml:7`, `configs/premium_stock_pool.yaml:12`
- SignalScorer 单测已补齐：`tests/test_signal_scorer.py`

### 3) 未达成项（阻断阶段四验收）

#### P0
1. 实时行情未接入
   - 现状：`scripts/data_adapter.py:114` 返回 `None/unavailable`
   - 影响：评分、波动和机会区间不可信

2. 源失败仍保留 fallback 产出
   - 现状：`scripts/ai_event_intel.py:246` 仍会生成 fallback item
   - 影响：生产链路存在“假数据冒充真实数据”风险

3. 静态价格依赖未移除
   - 现状：`configs/premium_stock_pool.yaml` 多处 `last_price`
   - 影响：支撑/阻力及入场区间滞后

#### P1
4. 多实例主裁决未落地
   - 现状：未发现 `IS_MASTER`/统一主节点裁决逻辑
   - 影响：跨实例可能出现事实不一致

5. EventBus 历史未落盘
   - 现状：`scripts/event_bus.py` 仅内存 `message_history`
   - 影响：重启后回放不完整

6. schema 默认版本不统一
   - 现状：
     - `scripts/ai_event_intel.py:288` 默认 `ai_intel_v1`
     - `scripts/ai_signal_adapter.py:116` 默认 `ai_factor_map_v1`
   - 影响：跨模块契约漂移

7. 语义级裁决未上线
   - 现状：未发现 AI 语义分析/链路选择模块
   - 影响：`trade` 类新闻仍存在误匹配风险

#### P2
8. websockets 弃用告警仍存在
   - 现状：pytest 输出 `DeprecationWarning`
   - 影响：后续依赖升级风险

### 4) 稳定性观察（本次审计过程）
- 首次测试出现 1 fail + 1 error（`tests/test_audit_center.py` 与 `.pytest_tmp` 目录冲突）。
- 复跑后恢复 154 通过，疑似临时状态干扰。
- 建议把测试临时目录策略改为更稳妥模式，避免偶发污染。

### 5) 通过标准（建议沿用）
- 功能面：真实源断开显式失败；禁止静默 fallback 产出
- 数据面：VIX/SPX/last_price 全部实时化
- 一致性：多实例主裁决 + 统一 event_id
- 质量面：CI 回归门禁覆盖误匹配率/方向一致性/P99
- 运维面：告警、回滚、证据台账可追溯

---

## 三、最终判定

- 阶段三：**通过（条件性）**
- 阶段四：**未通过（待 P0/P1 收口）**

建议：按 `阶段三完成度+阶段四规划.md` 的收尾清单执行，优先完成 P0 后再进入阶段四验收。
