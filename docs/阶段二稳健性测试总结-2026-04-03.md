# 阶段二稳健性测试总结（2026-04-03）

## 测试目标

验证“真实新闻监听 -> 评分 -> 风控 -> 执行回执”链路在以下三类场景中的可靠性：

1. 利好/利空对照分化
2. 高风险拦截与人工确认
3. AI失败降级（超时/错误）

## 测试环境

- 分支：`main`
- 执行模式：`paper`
- 关键模块：`NewsIngestion`、`EventEvidenceScorer`、`WorkflowRunner`、`RiskGatekeeper`、`ExecutionAdapter`

## A. 真实新闻链路联通结果

通过 RSS 实时抓取 3 条新闻，均完成从 A 层输入到执行层回执。

- 结果示例：
  - 新闻1：`EXECUTE`，`position_notional=4000`，`paper accepted`
  - 新闻2：`EXECUTE`，`position_notional=4000`，`paper accepted`
  - 新闻3：`EXECUTE`，`position_notional=4000`，`paper accepted`

结论：真实新闻到执行回执链路已贯通。

## B. 三组稳健性测试结果

### 1) 利好/利空对照（分化能力）

- `bullish_signal` -> `EXECUTE`，`score=71.35`，`position_notional=25000`，`execution_status=accepted`
- `bearish_signal` -> `WATCH`

判定：通过。系统可对信号强弱/方向给出差异化决策。

### 2) 高风险场景（硬闸能力）

- `liquidity_blackhole` -> `BLOCK`，首触发闸门 `G1`
- `manual_confirm_required`（未确认）-> `PENDING_CONFIRM`

判定：通过。高风险可被硬拦截，人工确认不可绕过。

### 3) 故障演练（降级能力）

- `ai_timeout_safe_default` -> `WATCH`，触发 `G7`
- `ai_error_safe_default` -> `BLOCK`，触发 `G7`

判定：通过。AI 超时/错误均可安全降级，不会绕过风控执行。

## C. 门禁结果

- `python3 -m pytest -q` -> `69 passed`
- `PYTHONPYCACHEPREFIX=/tmp/pycache python3 scripts/system_healthcheck.py` -> `OVERALL: GREEN`
- `bash scripts/verify_fullchain.sh` -> `passed`
- `bash scripts/verify_phase12.sh` -> `passed`

## D. 总体结论

阶段二当前状态可判定为：

- **工程与联调层面：合格**（链路完整、风控可用、降级有效、门禁通过）
- **实盘就绪层面：部分完成**（尚缺真实券商适配与实盘回报对账闭环）

## E. 下一步建议（实盘前）

1. 接入真实券商 `LiveBroker`（在现有 `BrokerAdapter` 抽象上实现）
2. 完成订单回报状态机（accepted/partial/filled/rejected/cancelled）
3. 增加重启后对账恢复（reconcile）与异常补偿
