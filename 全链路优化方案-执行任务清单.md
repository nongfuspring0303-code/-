# 全链路优化方案执行任务清单 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不破坏现有链路可运行性的前提下，完成“真实新闻源可验证 + 推荐质量提升 + 可观测门禁闭环”的全链路优化。

**Architecture:** 先做 P0（数据真实性与门禁口径），再做 P1（A 层语义与 B 层推荐），随后做 P2（前端可观测与 CI 准入）。每个任务均以“测试/门禁先行 + 最小代码改动 + 证据落盘”推进。

**Tech Stack:** Python 3, pytest, YAML, JSONL logs, GitHub Actions, Canvas (HTML/JS)

---

### Task 1: Canary 指标拆分（主源/回退分离统计）

**Files:**
- Modify: `scripts/canary_source_health.py`
- Modify: `tests/test_canary_source_health.py`

- [ ] **Step 1: 写失败测试（口径拆分）**

```python
def test_attempt_metrics_split_primary_vs_fallback():
    health = CanarySourceHealth()
    records = [
        {"fetch_status": "success", "is_primary_source": True, "new_item_count": 2, "fetch_latency_ms": 100, "freshness_lag_sec": 60},
        {"fetch_status": "success", "is_primary_source": False, "new_item_count": 3, "fetch_latency_ms": 200, "freshness_lag_sec": 120},
    ]
    metrics = health._attempt_metrics(records)
    assert metrics["primary_success_count"] == 1
    assert metrics["fallback_success_count"] == 1
```

- [ ] **Step 2: 运行失败测试确认红灯**

Run: `python3 -m pytest tests/test_canary_source_health.py -k split_primary_vs_fallback -v`
Expected: FAIL（字段不存在）。

- [ ] **Step 3: 最小实现主源/回退统计字段**

```python
# scripts/canary_source_health.py
primary_success_count = sum(1 for row in success_records if bool(row.get("is_primary_source")))
fallback_success_count = sum(1 for row in success_records if not bool(row.get("is_primary_source")))
```

- [ ] **Step 4: 再跑测试验证通过**

Run: `python3 -m pytest tests/test_canary_source_health.py -k split_primary_vs_fallback -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add scripts/canary_source_health.py tests/test_canary_source_health.py
git commit -m "feat: split canary metrics into primary and fallback channels"
```

### Task 2: Canary 报告显式标注回退模式

**Files:**
- Modify: `scripts/canary_source_health.py`
- Modify: `scripts/verify_canary_source_health.py`
- Modify: `tests/test_verify_canary_source_health.py`

- [ ] **Step 1: 写失败测试（回退模式提示）**

```python
def test_assessment_marks_fallback_mode_when_primary_failed():
    # 构造 summary: success 来自 fallback, primary 为 0
    assessment = health.assess(summary=summary, mode="prod")
    assert any("fallback_mode" in w for w in assessment.warnings)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python3 -m pytest tests/test_verify_canary_source_health.py -k fallback_mode -v`
Expected: FAIL。

- [ ] **Step 3: 最小实现 warning/evidence 字段**

```python
if primary_success_count == 0 and fallback_success_count > 0:
    warnings.append("fallback_mode_active: primary source unavailable")
```

- [ ] **Step 4: 复测**

Run: `python3 -m pytest tests/test_verify_canary_source_health.py -k fallback_mode -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add scripts/canary_source_health.py scripts/verify_canary_source_health.py tests/test_verify_canary_source_health.py
git commit -m "feat: surface fallback mode in canary verification output"
```

### Task 3: A 层低置信度分流（规则 -> LLM 升级接口，AI Trigger Supplement）

> **状态**: ✅ 已完成 (2026-04-10) | **2026-04-10 更新**: 已集成 GLM-4.7 Flash API

**Files:**
- Modify: `scripts/ai_semantic_analyzer.py`
- Modify: `configs/edt-modules-config.yaml`
- Test: `tests/test_ai_semantic_analyzer.py`

- [x] **Step 1: 写失败测试（低置信度触发 provider）**

```python
def test_semantic_calls_provider_when_confidence_below_threshold(monkeypatch):
    # 模拟规则输出低置信度，断言 provider 被调用
```

- [x] **Step 2: 跑测试确认失败**

Run: `python3 -m pytest tests/test_ai_semantic_analyzer.py -k confidence_below_threshold -v`
Expected: FAIL。

- [x] **Step 3: 实现 provider 抽象与配置开关**

```yaml
runtime:
  semantic:
    enabled: true
    provider: "glm-4.7-flash"
    model: "glm-4.7-flash"
```

- [x] **Step 4: 集成 GLM-4.7 Flash API（2026-04-10 新增）**

```python
# ai_semantic_analyzer.py 已实现真正的 GLM API 调用
# - 使用 ZAI API (api.z.ai) + glm-4.7-flash 模型
# - 发送金融新闻语义分析 prompt
# - 解析 JSON 响应
# - 遇 rate limit / timeout 时自动降级到 deterministic fallback
```

- [x] **Step 5: 跑测试**

Run: `python3 -m pytest tests/test_ai_semantic_analyzer.py -v`
Expected: PASS。

- [x] **Step 6: 提交**

```bash
git add scripts/ai_semantic_analyzer.py configs/edt-modules-config.yaml tests/test_ai_semantic_analyzer.py
git commit -m "feat: add low-confidence semantic provider fallback interface"
```

> **2026-04-10 追加提交**: 已推送 GLM API 集成到 PR #31

**验证记录（AI supplement 联合回归）**

- 命令：

```bash
python3 -m pytest tests/test_ai_semantic_analyzer.py tests/test_realtime_news_monitor.py tests/test_intel_modules.py tests/test_ai_event_intel.py tests/test_ai_event_intel_sina.py -q
```

- 结果：`41 passed in 4.13s`（含环境告警 `NotOpenSSLWarning`，不影响用例通过）

### Task 4: B 层机会推荐非空率增强

**Files:**
- Modify: `configs/premium_stock_pool.yaml`
- Modify: `scripts/opportunity_score.py`
- Modify: `tests/test_opportunity_score.py`

- [ ] **Step 1: 写失败测试（有板块时非空机会）**

```python
def test_opportunity_non_empty_when_sector_present():
    out = scorer.build_opportunity_update(payload_with_sector)
    assert len(out["opportunities"]) > 0
```

- [ ] **Step 2: 跑失败测试**

Run: `python3 -m pytest tests/test_opportunity_score.py -k non_empty_when_sector_present -v`
Expected: FAIL。

- [ ] **Step 3: 扩池 + 评分兜底规则最小实现**

```python
# 兜底：sectors 命中但候选为空时，按 canonical sector 从 premium pool 取 topN
```

- [ ] **Step 4: 复测**

Run: `python3 -m pytest tests/test_opportunity_score.py -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add configs/premium_stock_pool.yaml scripts/opportunity_score.py tests/test_opportunity_score.py
git commit -m "feat: improve opportunity fallback selection and pool coverage"
```

### Task 5: 新增实时性门禁脚本

**Files:**
- Create: `scripts/verify_news_freshness.py`
- Create: `tests/test_verify_news_freshness.py`

- [ ] **Step 1: 写失败测试（freshness 阈值）**

```python
def test_verify_news_freshness_fails_on_stale_data(tmp_path):
    # 构造旧时间样本，应返回 non-zero
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python3 -m pytest tests/test_verify_news_freshness.py -v`
Expected: FAIL。

- [ ] **Step 3: 实现脚本**

```python
# 读取 canary summary / event bus 时间，判定 P95 freshness_lag_sec <= 阈值
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python3 -m pytest tests/test_verify_news_freshness.py -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add scripts/verify_news_freshness.py tests/test_verify_news_freshness.py
git commit -m "feat: add freshness gate for realtime news evidence"
```

### Task 6: 新增推荐非空率门禁脚本

**Files:**
- Create: `scripts/verify_opportunity_non_empty_rate.py`
- Create: `tests/test_verify_opportunity_non_empty_rate.py`

- [ ] **Step 1: 写失败测试**

```python
def test_verify_non_empty_rate_fails_below_threshold(tmp_path):
    assert exit_code == 1
```

- [ ] **Step 2: 跑失败测试**

Run: `python3 -m pytest tests/test_verify_opportunity_non_empty_rate.py -v`
Expected: FAIL。

- [ ] **Step 3: 实现脚本**

```python
# 统计 event->sector->opportunity 链路中有板块样本的机会非空率
```

- [ ] **Step 4: 复测**

Run: `python3 -m pytest tests/test_verify_opportunity_non_empty_rate.py -v`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add scripts/verify_opportunity_non_empty_rate.py tests/test_verify_opportunity_non_empty_rate.py
git commit -m "feat: add opportunity non-empty-rate quality gate"
```

### Task 7: CI 集成新增门禁

**Files:**
- Modify: `.github/workflows/ci.yml`

- [ ] **Step 1: 写最小 CI 变更**

```yaml
- name: Verify canary freshness
  run: python3 scripts/verify_news_freshness.py

- name: Verify opportunity non-empty rate
  run: python3 scripts/verify_opportunity_non_empty_rate.py
```

- [ ] **Step 2: 本地 dry-run 检查 workflow 语法**

Run: `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml','r',encoding='utf-8')); print('ok')"`
Expected: `ok`

- [ ] **Step 3: 提交**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: enforce freshness and opportunity quality gates"
```

### Task 8: 文档与复核清单同步

**Files:**
- Modify: `项目全链路说明文档.md`
- Modify: `全链路优化方案.md`
- Modify: `同伴复核-checklist-20260409.md`

- [ ] **Step 1: 更新文档章节与命令**

```text
补充新增门禁脚本、指标阈值、回退模式判定口径。
```

- [ ] **Step 2: 更新复核清单**

```text
新增检查项：freshness gate、non-empty-rate gate、fallback_mode_active。
```

- [ ] **Step 3: 提交**

```bash
git add 项目全链路说明文档.md 全链路优化方案.md 同伴复核-checklist-20260409.md
git commit -m "docs: sync optimization gates and peer-review checklist"
```

### Task 9: 终验回归与发布前核验

**Files:**
- Verify: `scripts/verify_canary_source_health.py`
- Verify: `scripts/verify_news_freshness.py`
- Verify: `scripts/verify_opportunity_non_empty_rate.py`
- Verify: `scripts/system_healthcheck.py`

- [ ] **Step 1: 运行核心测试**

Run: `python3 -m pytest -q`
Expected: 全绿。

- [ ] **Step 2: 运行门禁脚本**

Run: `python3 scripts/verify_canary_source_health.py --refresh`
Expected: GREEN。

Run: `python3 scripts/verify_news_freshness.py`
Expected: 退出码 0。

Run: `python3 scripts/verify_opportunity_non_empty_rate.py`
Expected: 退出码 0。

- [ ] **Step 3: 运行系统健康检查**

Run: `python3 scripts/system_healthcheck.py --mode prod`
Expected: OVERALL GREEN。

- [ ] **Step 4: 提交终验证据**

```bash
git add logs/system_health_report.json logs/canary_live/canary_health_summary.json logs/canary_live/canary_health_report.json
git commit -m "chore: attach final health and canary verification evidence"
```

---

### Task 10: 新浪直播 API 接入（替代 NewsAPI）

> **状态**: ✅ 已完成 (2026-04-10)

**目标**: 将新浪财经直播 API 作为主新闻源，接替缺失的 NewsAPI

**Files:**
- Modify: `scripts/ai_event_intel.py`
- Modify: `configs/edt-modules-config.yaml`
- Modify: `scripts/canary_source_health.py`
- Create: `tests/test_ai_event_intel_sina.py`

- [x] **Step 1: 实现 NewsIngestion._fetch_sina() 方法**

```python
def _fetch_sina(self, timeout: int = 8) -> List[Dict[str, Any]]:
    """Fetch news from Sina live API."""
    enable_sina = bool(self._get_config("modules.NewsIngestion.params.enable_sina", False))
    if not enable_sina:
        return []
    # 获取配置参数、发起请求、解析 JSON、归一化返回
```

- [x] **Step 2: 在 execute() 中调用 Sina**

在 RSS 源遍历后添加 Sina 数据源，与 RSS 一起去重、排序

- [x] **Step 3: 配置 Sina 源**

```yaml
NewsIngestion:
  params:
    enable_sina: true
    sina:
      url: "http://zhibo.sina.com.cn/api/zhibo/feed"
      params:
        page: 1
        page_size: 20
        zhibo_id: 152
        dire: "f"
```

- [x] **Step 4: Canary 切换到 Sina**

```yaml
canary_source_health:
  source_id: "sina_live_feed"
  source_url: "http://zhibo.sina.com.cn/api/zhibo/feed"
  source_kind: "json"
  sources:
    - url: "http://zhibo.sina.com.cn/api/zhibo/feed"
      kind: "json"
      params:
        page: 1
        page_size: 10
        zhibo_id: 152
        dire: "f"
      headers:
        User-Agent: "Mozilla/5.0"
        Referer: "http://finance.sina.com.cn/7x24/"
```

- [x] **Step 5: 实现 Sina JSON 解析器**

在 `canary_source_health.py` 添加 `_parse_sina_json_items()` 方法

- [x] **Step 6: 验证门禁**

```bash
python3 scripts/verify_canary_source_health.py --refresh
# Expected: GREEN (success_rate=1.0, p95_latency<100ms, freshness_lag~0)
```

- [x] **Step 7: 运行测试**

```bash
python3 -m pytest tests/test_ai_event_intel_sina.py tests/test_ai_event_intel.py -v
# Expected: 全部 PASS
```

- [x] **Step 8: 提交**

```bash
git add scripts/ai_event_intel.py configs/edt-modules-config.yaml scripts/canary_source_health.py tests/test_ai_event_intel_sina.py
git commit -m "feat: integrate Sina live API as primary news source"
```

---

### Task 11: 文档同步（补充 Sina + GLM API 接入）

> **状态**: ✅ 已完成 (2026-04-10)

**Files:**
- Modify: `全链路优化方案-执行任务清单.md`
- Modify: `项目全链路说明文档.md`
- Modify: `同伴复核-checklist-20260409.md`

- [x] **Step 1: 同步 GLM 语义配置说明**
- [x] **Step 2: 同步 Sina 主源与 canary 门禁说明**
- [x] **Step 3: 提交并推送到 PR #31**

### Task 12: 启动链路与前端可观测补充（run_local 集成 + AI 展示）

> **状态**: ✅ 已完成 (2026-04-10)

**Files:**
- Modify: `scripts/run_c_module_stack.py`
- Modify: `scripts/realtime_news_monitor.py`
- Modify: `canvas/app.js`
- Modify: `canvas/styles.css`
- Modify: `schemas/event_update.yaml`
- Modify: `项目全链路说明文档.md`
- Modify: `同伴复核-checklist-20260409.md`

- [x] **Step 1: `run_local.sh` 一键启动内置新闻监控**
  - 新增参数：`--no-news`、`--news-interval`
  - 默认开启新闻轮询（30 秒）
- [x] **Step 2: 前端展示 AI 语义字段**
  - 事件载荷补充 `ai_verdict` / `ai_confidence` / `ai_reason`
  - Canvas 新闻卡新增 AI 分析展示区
- [x] **Step 3: 修复“预览事件不含 AI 字段”问题**
  - 预览消息二次更新同一 `trace_id`，补齐 AI 结果
- [x] **Step 4: 提交并推送到 PR #31**
