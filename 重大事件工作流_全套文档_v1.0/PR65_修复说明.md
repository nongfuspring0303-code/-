# PR #65 修复说明

本文档用于说明本次针对 PR #65 审阅意见的修复内容，便于复核与审计。

## 修复摘要

本次已完成以下修复：

1. 修复 `EventObject` 与任务总书字段不一致问题（`evidence_spans`）。
2. 修复 `time_window` 与附录 B 枚举口径不一致问题。
3. 补齐动作层枚举覆盖（`execution_window`、`best_setup`）。
4. 修正路径分层描述与附录 C 口径冲突（P3 不再直接放弃）。
5. 补齐 ActionCard 风险层契约字段 `blockers`。

## 逐项对应审阅意见

### 1) MAJOR：`evidence_spans` 契约不一致

- 修复方式：在骨干代码文档 `EventObject` 增加 `evidence_spans: List[str]`。
- 结果：与任务总书成员 A 交付字段一致，消除同 PR 内部契约歧义。

### 2) MAJOR：`time_window` 枚举不完整

- 修复方式：将时间窗口扩展为：
  - `0-24h`
  - `24-48h`
  - `48-72h`
  - `72-120h`
  - `>120h`
- 结果：与附录 B 的窗口口径对齐。

### 3) MINOR：动作枚举覆盖不足

- 修复方式：
  - 新增执行窗口推导，覆盖 `open / intraday / close_near / next_day_watch`。
  - 调整 `best_setup`，在 `Peak/Fading` 场景输出 `no_chase`。
- 结果：动作语义与附录 B/F 约束一致。

## 额外对齐项

### 路径分层口径

- 将“P3/P4 直接放弃”修正为：
  - P3：观察层保留（不入主机会池）
  - P4：直接放弃

### ActionCard 风险层字段

- 风险层补充 `blockers`，并在任务总书字段表同步声明。

## 影响范围

- `收敛执行计划_总纲.md`
- `收敛执行计划_任务总书.md`
- `收敛执行计划_骨干代码.md`

## 说明

本次提交为文档层修复，不包含运行时代码逻辑变更。
