# Phase 4 配置落库检查清单

## 1. 放置路径
将以下文件放入仓库 `configs/` 目录：
- `configs/gate_policy.yaml`
- `configs/metric_dictionary.yaml`
- `configs/backtest_protocol.yaml`

## 2. 落库前核查
- [ ] 当前分支已确认，不在成员B受限边界分支内误改。
- [ ] 已明确这 3 个文件由你本人或授权角色负责维护。
- [ ] 仓库当前不存在同名旧文件，或已完成差异比对。

## 3. YAML 语法检查
在仓库根目录执行：

```bash
python3 - <<'PY'
import yaml, pathlib
for p in [
    'configs/gate_policy.yaml',
    'configs/metric_dictionary.yaml',
    'configs/backtest_protocol.yaml',
]:
    with open(p, 'r', encoding='utf-8') as f:
        yaml.safe_load(f)
    print('OK', p)
PY
```

- [ ] 三个文件均可被 `yaml.safe_load` 正常解析。

## 4. 字段级核查
### gate_policy.yaml
- [ ] 存在 `asset_validation.no_action_max`
- [ ] 存在 `asset_validation.trade_min`
- [ ] 存在 `path_dominance.min_gap`
- [ ] 存在 `mixed_regime_override.enabled`
- [ ] 存在 `trade_admission.*` 七项准入字段
- [ ] 存在 `signal_grade.max_stock_candidates: 5`
- [ ] 存在 `shadow.*` 阈值组
- [ ] 存在 `rollback.*` 回滚阈值组

### metric_dictionary.yaml
- [ ] 存在 `A0`
- [ ] 存在 `A0_5`
- [ ] 存在 `confidence`
- [ ] 存在 `asset_validation.score`
- [ ] 存在 `path_dominance.score`
- [ ] 存在 `final_sector_score`
- [ ] 存在 `stock_transmission_score`
- [ ] 默认 rounding = 2 位小数
- [ ] 关键枚举已注册

### backtest_protocol.yaml
- [ ] `pit_snapshots.required` 四项齐全
- [ ] `timestamps.ordering_constraints` 存在
- [ ] `feature_visibility` 限制已写入
- [ ] `splitting.method = walk_forward`
- [ ] `purge_enabled = true`
- [ ] `embargo_enabled = true`
- [ ] 成本/滑点/流动性约束已写入
- [ ] 有 `event_outcome_validation`
- [ ] 有 `stock_alpha_validation`
- [ ] 有 `robustness` 规则

## 5. 仓库引用点检查
用全文搜索确认代码里有没有读取这些键：

```bash
grep -R "gate_policy" -n .
grep -R "metric_dictionary" -n .
grep -R "backtest_protocol" -n .
```

- [ ] 读取路径统一通过 ConfigCenter，而不是代码硬编码。
- [ ] 没有新的阈值被直接写进 Python/JS 文件。
- [ ] 没有出现与 YAML 重复定义的魔法数字分叉。

## 6. 与制度文件一致性核查
- [ ] `path_dominance.min_gap = 12.0`
- [ ] `asset_validation.trade_min = 65.0`
- [ ] `trade_admission.A0_min = 60.0`
- [ ] `trade_admission.confidence_min = 65.0`
- [ ] `signal_grade.max_stock_candidates = 5`
- [ ] `shadow.action_match_rate_min = 0.95`
- [ ] `shadow.path_match_rate_min = 0.90`
- [ ] `shadow.sector_match_rate_min = 0.90`
- [ ] `shadow.score_delta_p95_max = 8.0`
- [ ] `rollback.action_match_breach_days = 2`

## 7. 提交前提醒
- [ ] 这 3 个文件属于项目级配置补齐，不要再以成员B越界身份提交。
- [ ] 提交说明中明确：这是“配置单一真源补齐”，不是业务逻辑扩写。
- [ ] 提交后再恢复成员B Wave-2 / Wave-3 推进。

## 8. 当前仍需注意的额外缺口
根据当前远端主仓库状态，除了这 3 个文件外，`configs/asset_baskets.yaml` 也尚未存在；如果后续要恢复成员B资产验证层开发，这个文件仍需要单独补齐。
